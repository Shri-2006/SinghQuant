# SinghQuant: FULL install (every optional component). For a CPU-only
# paper-trading node use `bash setup.sh` (core + dev) and add flags for what
# you need; see setup.sh.
#
#   requirements-core.txt        paper-trading runtime (stable, risky1, ledger, execution)
#   requirements-dev.txt         pytest
#   requirements-rl.txt          risky2 PPO (torch from the CPU wheel index)
#   requirements-backtest.txt    vectorbt
#   requirements-dashboard.txt   streamlit
#   requirements-optional.txt    google-genai macro fallback
#   requirements-py311-pinned.txt original exact pins of the last deployment (Python 3.11)
#
# alpaca-trade-api 3.0.0 is unmaintained and pins websockets<11 / aiohttp==3.8.2,
# which do not build on Python 3.12+; setup.sh installs it with --no-deps.
-r requirements-core.txt
-r requirements-dev.txt
-r requirements-dashboard.txt
-r requirements-backtest.txt
-r requirements-optional.txt
-r requirements-rl.txt
